entry class MainClass:
    private field a int;
    function main() returns int:
        var a = 1;
        while( a > 2 ) begin
            print( a + 1);
            a = a + 1;
        end
        return 0;
    end
end

class binarySearch:
    private field a int;
    function main() returns int:
        var a = 1;
        while( a > 2 ) begin
            print( a + 1);
            a = a + 1;
        end
        return 0;
    end
end
